# 可选：deepseek、moonshot 或 gemini。
# 中国大陆学生应使用 deepseek 或 moonshot（Kimi）。
# Streamlit 首页保存后会立即生效，手工修改会在后续模型调用时读取。
# 如果进程环境中已有同名变量，则以进程环境为准。
MODEL_PROVIDER=deepseek

# 可以同时填写多个 Key；程序只检查 MODEL_PROVIDER 选中的一项。
DEEPSEEK_API_KEY=
MOONSHOT_API_KEY=
GEMINI_API_KEY=
